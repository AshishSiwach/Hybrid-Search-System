[pytest]
# importlib import mode is the modern default — avoids legacy
# walk-up-for-parent-package behaviour. Required for the querylens
# package layout to be discovered cleanly.
addopts = --import-mode=importlib
testpaths = tests
python_files = test_*.py
